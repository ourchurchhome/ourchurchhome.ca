---
export const prerender = false;

import { clearSessionCookie } from '../../cms/lib/session';

return new Response(null, {
  status: 302,
  headers: {
    Location: '/cms/login',
    'Set-Cookie': clearSessionCookie(),
  },
});
---

